<% var body = include('./partials/edit_body', { title, flash, event, isEdit }) %>
<% var scripts = include('./partials/scripts', { title, flash, event, isEdit }) %>

<%- include('../layouts/app', { title, flash, body, scripts}) %> 
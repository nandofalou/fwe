<% var body = include('./partials/edit_body', { title, flash, device, isEdit }) %>
<% var scripts = include('./partials/scripts', { title, flash, device, isEdit }) %>

<%- include('../layouts/app', { title, flash, body, scripts}) %> 
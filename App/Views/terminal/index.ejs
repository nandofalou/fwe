<% var body = include('./partials/body', { title, flash, filter }) %>
<% var scripts = include('./partials/scripts', { title, flash, filter }) %>

<%- include('../layouts/app', { title, flash, body, scripts}) %> 
<% var body = include('./partials/body', { title, flash, events, categories, filter }) %>
<% var scripts = include('./partials/scripts', { title, flash, events, categories, filter }) %>

<%- include('../layouts/app', { title, flash, body, scripts}) %> 